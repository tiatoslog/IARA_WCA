# Esta rodada foi INTERROMPIDA — não é evidência de nada

Subida às 10:11 de 20/08/2026 sem `--cerebro`, o que faz a campanha usar o
padrão (`ollama` local). O Ollama não está no ar nesta máquina, então todo turno
cognitivo caiu em `nuvem_indisponivel: true` e `origem_raciocinio: "local"` —
ou seja, as missões de conversa mediram o motor SEM cérebro.

Interrompida na 19ª de 49 missões, ao ser percebido que a configuração não
certificava o Gate 3. Não há `RELATORIO.md` nem `veredito.json`: a pasta guarda
só o protocolo bruto do que chegou a rodar.

**Não ingira esta pasta como medição.** A rodada válida com cérebro real é
`CAMPANHA-2026-08-20-1029-anthropic-3091` (antes das correções) e
`CAMPANHA-2026-08-20-1124-anthropic-3121` (depois).
