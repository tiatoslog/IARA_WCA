# docs/especificacao/

A especificação do projeto, como documento de origem.

`IARA_Especificacao_v2.md` é a versão em Markdown. O `.docx` original está em
`arquivos/documentos/IARA_Especificacao_do_Projeto.docx`.

**Isto não é a documentação do sistema.** É o que se pretendia construir. Para o
que existe hoje, veja `docs/gerado/`. Divergência entre os dois é esperada e
informativa: mostra o que mudou de ideia durante a construção.
